{
  "name": "cursor-history",
  "author": "lumine-code",
  "version": "1.0.0",
  "description": "Navigate backward and forward through recent cursor positions.",
  "keywords": [
    "navigation",
    "jumplist",
    "location",
    "movement"
  ],
  "repository": "https://github.com/lumine-code/cursor-history",
  "bugs": {
    "url": "https://github.com/lumine-code/cursor-history/issues"
  },
  "license": "MIT",
  "main": "./lib/main",
  "files": [
    "keymaps",
    "lib",
    "menus",
    "spec",
    "styles"
  ],
  "engines": {
    "lumine": "^1.0.0"
  },
  "backgroundTips": [
    "You can return to an earlier cursor position with {{ 'cursor-history:prev' | keystroke }}",
    "You can move forward through cursor history with {{ 'cursor-history:next' | keystroke }}"
  ],
  "scripts": {
    "test": "lumine --test spec",
    "lint": "eslint . --max-warnings 0",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "configSchema": {
    "max": {
      "title": "Maximum Entries",
      "description": "Maximum number of cursor positions to keep.",
      "type": "integer",
      "minimum": 1,
      "default": 100
    },
    "rowDeltaToRemember": {
      "title": "Row Movement Threshold",
      "description": "Save a position when the cursor moves farther than this many rows.",
      "type": "integer",
      "minimum": 0,
      "default": 4
    },
    "columnDeltaToRemember": {
      "title": "Column Movement Threshold",
      "description": "Save same-row movement when it exceeds this many columns.",
      "type": "integer",
      "minimum": 0,
      "default": 9999
    },
    "excludeClosedBuffer": {
      "title": "Exclude Closed Files",
      "description": "Remove positions whose editor has been closed.",
      "type": "boolean",
      "default": false
    },
    "keepSingleEntryPerBuffer": {
      "title": "One Entry per File",
      "description": "Keep only the latest position for each file.",
      "type": "boolean",
      "default": false
    },
    "searchAllPanes": {
      "title": "Search All Panes",
      "description": "Reuse an existing editor from any pane before opening another one.",
      "type": "boolean",
      "default": true
    },
    "openInPendingState": {
      "title": "Open in Preview Mode",
      "description": "Open closed history targets in pending state.",
      "type": "boolean",
      "default": false
    },
    "flashOnLand": {
      "title": "Flash Destination Line",
      "description": "Briefly highlight the destination after a jump.",
      "type": "boolean",
      "default": true
    },
    "ignoreCommands": {
      "title": "Ignored Commands",
      "description": "Commands that must not create history entries.",
      "type": "array",
      "items": {
        "type": "string"
      },
      "default": [
        "command-palette:toggle"
      ]
    },
    "debug": {
      "title": "Debug Logging",
      "description": "Print history changes to the developer console.",
      "type": "boolean",
      "default": false
    }
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.8.0",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-n": "^18.2.2",
    "globals": "^17.9.0",
    "prettier": "^3.9.6"
  }
}
